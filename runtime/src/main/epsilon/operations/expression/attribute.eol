@cached
operation EXPR!AttributeSelector getAttributeType() {
    var objectType = self.objectExpression.getObjectType(modelAdapter);
    
    if (objectType.isDefined()) {
        return modelAdapter.getAttributeType(objectType, self.attributeName);
    } else {
        return null;
    }
}
