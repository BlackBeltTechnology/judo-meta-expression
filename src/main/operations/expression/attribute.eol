@cached
operation EXPR!AttributeReference getAttributeType() {
    var objectType = self.objectExpression.getObjectType();
    
    if (objectType.isDefined()) {
        return objectType.getAttributeType(self.attributeName);
    } else {
        return null;
    }
}
